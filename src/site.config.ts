/**
 * Published contact address — the privacy policy's required point of contact
 * and the sidebar's feedback link. One export so the two pages can't publish
 * different addresses.
 *
 * Worth moving to a role address (hello@ / privacy@) once the domain has
 * mail, so a personal inbox isn't published on a public page forever.
 */
export const CONTACT_EMAIL = 'ibrahim.alrayany@gmail.com';
