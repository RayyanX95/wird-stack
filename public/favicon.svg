<!--
  App mark: the eight-point khatim star with a check cut out of it.

  The star is two identical squares, one rotated 45 degrees — in Islamic
  geometry, order built from a single shape repeated; it is also the Rub el
  Hizb, the marker dividing the Qur'an into equal portions for regular daily
  recitation. Structure through repetition, which is what habit-building is.
  The check in negative space is what makes it this app's mark rather than a
  general motif: a habit kept.

  Deliberately tied to the IDEA, not the name: the app may be renamed, so no
  letterform or wordmark. Deliberately not a crescent/star, which reads as a
  generic "Muslim app" badge (and is really a national flag symbol).

  Notes for anyone adjusting this:

  - CHECK SIZE IS CONSTRAINED. The star's inner octagon has an inradius of
    8.49 from centre (16,16). The check's furthest point currently reaches
    6.83, leaving 1.66 of white clearance. Grow it past ~7.4 and the tips
    touch the notches, the white ring is severed, and the eight-point
    silhouette stops reading — it becomes a check on green blobs. This has
    actually happened once; the numbers above are measured, not guessed.

  - The check is built from filled polygons plus three circles faking round
    caps. It is NOT a stroked path, and tidying it into one will break the
    build: ImageMagick renders this file to favicon.ico and silently drops
    stroked paths, so the check would vanish from the .ico fallback while
    still looking correct in the SVG. Verified, not assumed.

  - Keep the whole mark SOLID. Earlier versions built from dots and hairline
    strokes all dissolved or turned muddy at 16px.

  - The two star squares share one circumradius (12). Different radii turn the
    even eight-point star into a lopsided pinwheel.

  - Artwork runs edge to edge with no corner radius; the browser and OS round
    the tile. Adding rx here leaves transparent corners that show as white
    notches in a browser tab. Where the mark needs rounded corners (the
    sidebar), that comes from CSS border-radius on the host element.

  - The fill is the literal value of the accent token rather than a var()
    reference, because this file is also served standalone as the favicon,
    where there is no CSS context to resolve a custom property against. If the
    accent token changes in tokens.css, change it here too.

  - Careful writing these comments: XML forbids a double hyphen inside a
    comment, so the token names here are written without their leading dashes.
    Slipping one in makes the file invalid XML, which browsers parse strictly
    for SVG and which silently breaks the favicon.ico generation.

  THIS FILE IS THE SINGLE SOURCE OF TRUTH for the mark. It is imported inline
  by AppSidebar.vue, and `npm run logo` regenerates public/favicon.svg and
  public/favicon.ico from it. Don't hand-edit those; edit this and re-run.
-->
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <rect width="32" height="32" fill="#1f6f5c" />

  <polygon points="7.51,7.51 24.49,7.51 24.49,24.49 7.51,24.49" fill="#ffffff" />
  <polygon points="16,4 28,16 16,28 4,16" fill="#ffffff" />

  <polygon points="10.98,17.66 13.70,20.38 16.38,17.70 13.66,14.98" fill="#1f6f5c" />
  <polygon points="16.50,20.26 21.46,14.34 18.54,11.90 13.58,17.82" fill="#1f6f5c" />
  <circle cx="12.32" cy="16.32" r="1.9" fill="#1f6f5c" />
  <circle cx="15.04" cy="19.04" r="1.9" fill="#1f6f5c" />
  <circle cx="20" cy="13.12" r="1.9" fill="#1f6f5c" />
</svg>
